# 릴리즈 노트

## 1.0.2

### 앱스토어 (한국어)

악보를 보며 계이름을 말하면 음표로 들어갑니다
메트로놈에 맞춰 말하면 박자까지 함께 잡힙니다
악보를 텍스트로 붙여넣어 오선지를 채웁니다
피아노에서 쉼표를 길이별로 바로 넣습니다
방금 넣은 음표는 한 번에 지울 수 있습니다

### App Store (English)

Say note names, see them on the staff
Speak in time and the rhythm follows
Paste a score written as plain text
Every rest length, right on the piano
Undo the note you just added

### 개발 메모 (스토어에 올리지 않음)

- **계이름 받아쓰기** — 상단 마이크 버튼. 카운트인 1마디 뒤 2마디를 듣고 자동 정지한다.
  인식 단어의 타임스탬프를 박으로 환산해 격자에 스냅하므로 멜로디와 박자가 한 번에 들어온다.
  스냅 단위(4/8/16분·리듬 없이), 시작 옥타브, 옥타브 자동 맞춤을 고를 수 있다.
  인식은 `requiresOnDeviceRecognition = true` 로 고정 — 개인정보처리방침의 "완전 오프라인"을 지키기 위함.
- **텍스트로 악보 붙여넣기** — 상단 클립보드 버튼. AI에게 규칙(예: `4옥도8분음`)대로 적어 달라고 한 뒤 붙여넣는다.
  규칙 전문 복사, 현재 악보를 텍스트로 복사 지원.
- **피아노 툴 스트립** — 쉼표 5종(온·2·4·8·16분)을 음표 값과 나란히 상시 노출, 한 번 탭으로 삽입.
  되돌리기(백스페이스) 추가.
- **음이름 표기** — 화면 표시를 계이름(도레미)에서 음이름(C D E)으로 통일. 음성 입력은 계이름·영문 둘 다 받는다.
- LeeoKit 2.6.0 → 3.7.1. `LeeoAppSpec` 에 `legal`·`monetization` 이 필수가 되어 `StaffSingerSpec` 에 추가했다.

---

## 1.0.1

초견 연습을 더 편하게 만드는 입력·표기 기능을 추가했습니다.

### 새로운 기능
- **음자리표 3종 지원** — 높은음자리표에 더해 **가온음자리표(다, C clef)** 와 **낮은음자리표(바, F clef)** 를 추가했습니다. 설정에서 바꿀 수 있습니다.
- **가상 피아노 건반** — 상단 🎹 버튼으로 2옥타브 피아노를 띄워 음을 탭으로 입력합니다. 흰 건반에 계이름(도레미)이 표시되고 옥타브 이동 버튼이 있습니다. 건반을 열면 오선이 위로 올라가 가려지지 않고, 음길이·점·셋잇단을 건반 위에서 바로 고를 수 있습니다.
- **셋잇단음표(triplet)** — 음표 줄의 **‘3’ 토글**을 켜고 음을 넣으면 셋잇단음표로 입력되고, 한 박에 세 음이 묶여 ‘3’ 표시가 붙습니다.
- **쉼표 입력 개선** — 쉼표 모드에서 음길이 줄이 5종 쉼표 버튼(온·2·4·8·16분)으로 바뀌어 원하는 길이를 한 번에 넣을 수 있고, 하나 넣으면 자동으로 음표 모드로 돌아갑니다.

### 개선
- **설정이 사이드 팔레트로** — 설정 화면이 악보를 가리지 않도록 한쪽에서 슬라이드되는 팔레트로 바뀌었습니다. **왼손잡이용으로 좌/우 전환**도 됩니다. (가로모드에서 위가 잘리던 문제도 해결)
- **마지막 음표 설정 유지** — 마지막에 고른 음 길이와 점음표 설정이 다음 실행에도 그대로 유지됩니다.
- **메트로놈 끄기 아이콘 정리** — 음소거 상태가 또렷이 보이도록 사선 표시를 다듬었습니다.

---

### App Store “이번 버전의 새로운 기능” (붙여넣기용)

```
• 가온음자리표·낮은음자리표 추가 (설정에서 변경)
• 가상 피아노 건반으로 간편 입력 (계이름 표시·옥타브 이동)
• 셋잇단음표 입력 ('3' 토글)
• 쉼표 입력 개선 (5종 쉼표 한 번에)
• 설정을 사이드 팔레트로 — 악보를 가리지 않고 왼손잡이 좌/우 전환 지원
• 마지막에 고른 음표 길이를 기억
• 메트로놈 음소거 아이콘 정리
```
